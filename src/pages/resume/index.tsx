import React, { useEffec } from 'react'

import Layout from '@theme/Layout'

// import clsx from 'clsx'

const TITLE = 'Resume'
const DESCRIPTION = "Tenn's resume page"

function Resume() {
  return (
    <Layout title={TITLE}>
      <main>
        <h1>Resume page</h1>
      </main>
    </Layout>
  )
}

export default Resume
